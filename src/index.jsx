import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

const rootNode = document.getElementById('react-root')
// render the whole app @root node
ReactDOM.render(
  <App />,
  rootNode
)
